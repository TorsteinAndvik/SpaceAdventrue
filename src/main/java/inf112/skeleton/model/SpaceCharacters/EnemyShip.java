package inf112.skeleton.model.SpaceCharacters;

public class EnemyShip extends SpaceShip {
    public EnemyShip(String name, String description, int hitPoints, int x, int y, int mass, int speed, float angle, int radius) {
        super(name, description, hitPoints, x, y, mass, speed, angle, radius);
    }
}
